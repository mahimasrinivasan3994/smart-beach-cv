# Water Segmentation

This model will detect water zones in given image

To install requirements run:

- pip install -r requirements.txt

To train the model run the command:

- python training.py

To test the model run the command:

 - python testing.py
